<%= form_for(current_user.following.build(following_id: user.id)) do |f| %>
  <div><%= f.hidden_field :follower_id, :value=>user.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary follow-btn" %>
<% end %>