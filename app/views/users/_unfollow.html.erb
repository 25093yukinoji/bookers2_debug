<%= form_for(current_user.following.find_by(follower_id: user.id), html: { method: :delete }) do |f| %>
    <div><%= f.hidden_field :follower_id, :value=>user.id %></div>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>